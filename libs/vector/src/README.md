# vector/src

Source and header files for the `vector` library.

| File | Description |
|---|---|
| `vector.hpp` / `vector.cpp` | `Vector` type, `magnitude`, `unitVector`, `dotProduct`, `almostParrallel` |
| `streamLine.hpp` / `streamLine.cpp` | `StreamLine` type holding an ordered path of grid coordinates |
