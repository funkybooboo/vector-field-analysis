# vector/src

Source and header files for the `vector` library.

| File | Description |
|------|-------------|
| `vector.hpp` / `vector.cpp` | `Vec2` type; `magnitude`, `unitVector`, `dotProduct`, `almostParallel` |
| `streamline.hpp` / `streamline.cpp` | `Streamline` type - an ordered path of grid coordinates traced through a vector field |
